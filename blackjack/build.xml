<?xml version="1.0" encoding="UTF-8"?>
<project name="blackjack" default="run" basedir=".">


    <property name="app.name" value="blackjack"/>
    <property name="app.version" value="0.1"/>
    <property name="app.fullname" value="${app.name}-${app.version}"/>
    <property name="build.home" value="${basedir}/build"/>
    <property name="dist.home" value="${basedir}/dist"/>
    <property name="docs.home" value="${basedir}/docs"/>
    <property name="src.home" value="${basedir}/src"/>
    <property name="lib.home" value="${basedir}/lib"/>
    <property name="javac.encoding" value="UTF-8"/>
  

    <!-- ==================== Compile options =========================== -->
  
    <property name="compile.debug" value="true"/>
    <property name="compile.deprecation" value="false"/>
    <property name="compile.optimize" value="true"/>

           
    <!-- ==================== Compilation Classpath =========================== -->

    <path id="compile.classpath">
        <fileset dir="${lib.home}">
            <include name="**/*.jar"/>
        </fileset>
    </path>

    <!-- ==================== All Target ====================================== -->


    <target name="all" depends="clean,compile" description="Clean build and dist directories, then compile"/>



    <!-- ==================== Clean Target ==================================== -->

    <target name="clean" description="Delete old build and dist directories">
        <delete dir="${build.home}"/>
        <delete dir="${dist.home}"/>
    </target>



    <!-- ==================== Compile Target ================================== -->

    <target name="compile" depends="prepare" description="Compile Java sources">

        <javac srcdir="${src.home}" destdir="${build.home}" debug="${compile.debug}" deprecation="${compile.deprecation}" optimize="${compile.optimize}" encoding="${javac.encoding}" includeantruntime="false">

       
            <classpath refid="compile.classpath"/>
   
        </javac>

        <!-- Copy application resources -->
        <copy todir="${build.home}">
            <fileset dir="${src.home}" excludes="**/*.java"/>
        </copy>

    </target>

    <!-- ==================== Prepare Target ================================== -->

    <target name="prepare">

        <mkdir dir="${build.home}"/>
		
        <mkdir dir="${dist.home}"/>

		
    </target>
  


    <!-- ==================== Dist Target ===================================== -->


    <target name="dist" depends="clean,compile,javadoc" description="Create binary distribution">
		
		
        <copy todir="${dist.home}/lib">
            <fileset dir="${lib.home}"/>
        </copy>
		
		
        <!-- Create application JAR file -->
        <jar jarfile="${dist.home}/${app.fullname}.jar" basedir="${build.home}">
            <manifest>
                <attribute name="Main-Class" value="blackjack.MainClass"/>
                <attribute name="Class-Path" value="lib/cartes-0.1.jar"/>                
            </manifest>
        </jar>

    </target>
  

    <!-- ==================== Javadoc Target ================================== -->


    <target name="javadoc" depends="compile" description="Create Javadoc API documentation">

        <mkdir dir="${dist.home}/docs/api"/>
		
		
        <javadoc sourcepath="${src.home}" destdir="${dist.home}/docs/api" packagenames="*">
			
            <classpath>
                <fileset dir="${lib.home}">
                    <include name="**/*.jar"/>
                </fileset>
            </classpath>
			
        </javadoc>
		

    </target>



    <!-- ==================== Run Targets ================================== -->

    <!-- Run solo (sans robot) -->
    <target name="run" depends="dist" description="Lance le jeu en mode solo (sans robot)">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true"/>
    </target>

    <!-- Run avec robot - stratégie simple -->
    <target name="run-robot" depends="dist" description="Lance avec robot (stratégie simple)">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true">
            <arg value="robot"/>
        </java>
    </target>

    <!-- Run avec robot - stratégie simple (explicite) -->
    <target name="run-robot-simple" depends="dist" description="Lance avec robot (stratégie simple)">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true">
            <arg value="robot"/>
            <arg value="simple"/>
        </java>
    </target>

    <!-- Run avec robot - stratégie optimale -->
    <target name="run-robot-optimal" depends="dist" description="Lance avec robot (stratégie optimale)">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true">
            <arg value="robot"/>
            <arg value="optimal"/>
        </java>
    </target>

    <!-- Run avec arguments personnalisés via -Dargs="..." -->
    <target name="run-custom" depends="dist" description="Lance avec arguments personnalisés (ant run-custom -Dargs='robot optimal')">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true">
            <arg line="${args}"/>
        </java>
    </target>

    <!-- Run English (sans robot) -->
    <target name="run-english" depends="dist" description="Lance en anglais">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true">
            <jvmarg value="-Duser.language=en"/>
        </java>
    </target>

    <!-- Afficher l'aide -->
    <target name="run-help" depends="dist" description="Affiche l'aide du jeu">
        <java jar="${dist.home}/${app.fullname}.jar" fork="true">
            <arg value="help"/>
        </java>
    </target>





</project>